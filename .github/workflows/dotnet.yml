name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Pack
      run: dotnet pack --configuration Release
#    - name: Upload a Build Artifact
#      uses: actions/upload-artifact@v2.2.4
#      with:
#        name: artifacts
#        path: ./src/Rollcall.Extensions.Microsoft.DependencyInjection/bin/Release/*nupkg
    - name: Push nupkg
      run: dotnet nuget push ./src/Rollcall.Extensions.Microsoft.DependencyInjection/bin/Release/*.nupkg -k ${{ secrets.ROLLCALLNUGET }} -s https://api.nuget.org/v3/index.json
    - name: Push snupkg
      run: dotnet nuget push ./src/Rollcall.Extensions.Microsoft.DependencyInjection/bin/Release/*.snupkg -k ${{ secrets.ROLLCALLNUGET }} -s https://api.nuget.org/v3/index.json      
